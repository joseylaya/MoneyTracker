import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';
import InputError from '@/Components/InputError';
import { Head, Link, useForm } from '@inertiajs/react';
import { ChevronLeft, WalletCards } from 'lucide-react';

export default function Create() { const { data, setData, post, processing, errors } = useForm({ name: '', description: '', currency_code: 'PHP' }); return <AuthenticatedLayout header={<div className="flex items-center gap-3"><Link href={route('trackers.index')} className="flex size-10 items-center justify-center"><ChevronLeft size={28}/></Link><h1 className="font-display text-xl font-bold">Create Tracker</h1></div>}><Head title="Create Tracker"/><form onSubmit={(event) => {event.preventDefault();post(route('trackers.store'));}} className="page-wrap max-w-xl"><div className="rounded-[2rem] border border-[#bcefd1] bg-[#edfcf3] p-7 text-center"><span className="mx-auto flex size-16 items-center justify-center rounded-2xl bg-white text-[#2ecc70]"><WalletCards size={30}/></span><h2 className="mt-5 font-display text-2xl font-bold">A shared place for every expense</h2><p className="mt-2 text-sm text-slate-500">You&apos;ll be the owner and can invite people once it&apos;s created.</p></div><div className="mt-8 space-y-6"><div><label className="eyebrow">Tracker name</label><input autoFocus value={data.name} onChange={(event) => setData('name', event.target.value)} placeholder="e.g. Siargao trip" className="ss-input mt-3"/><InputError message={errors.name} className="mt-2"/></div><div><label className="eyebrow">What are you sharing? <span className="normal-case tracking-normal">(optional)</span></label><textarea value={data.description} onChange={(event) => setData('description', event.target.value)} placeholder="A short description" rows="3" className="ss-input mt-3 h-auto py-4"/><InputError message={errors.description} className="mt-2"/></div><div><label className="eyebrow">Currency</label><select value={data.currency_code} onChange={(event) => setData('currency_code', event.target.value)} className="ss-input mt-3"><option value="PHP">Philippine peso (₱)</option><option value="USD">US dollar ($)</option><option value="EUR">Euro (€)</option></select><InputError message={errors.currency_code} className="mt-2"/></div></div><button disabled={processing} className="ss-button mt-9 w-full text-lg">{processing ? 'Creating…' : 'Create Tracker'}</button></form></AuthenticatedLayout>; }
